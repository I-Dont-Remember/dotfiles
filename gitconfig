[user]
	email = 8552248+I-Dont-Remember@users.noreply.github.com
	name = Kevin Quinn
[color]
	ui = true
[core]
	editor = vim
	autocrlf = false
[branch]
    sort = -committerdate
[alias]
	df = diff
	di = diff

	# short form, with colors and branch/tag annotations
	ls = log --pretty=format:"%C(yellow)%h%Cred%d\\ %Creset%s%Cblue\\ [%cn]" --decorate
	
	# no color version
	lnc = log --pretty=format:"%h\\ %s\\ [%cn]"

[pull]
	rebase = true

[init]
	defaultBranch = main

[push]
	autoSetupRemote = true

[core]
	excludesFile = ~/.gitignore_global

[alias]
	lg = log --oneline --graph --decorate --all

# delta diff pager — uncomment to enable (requires delta to be installed)
# [core]
# 	pager = delta
# [interactive]
# 	diffFilter = delta --color-only
# [delta]
# 	navigate = true
# 	side-by-side = true
